# 🌟 버킷리스트 프로젝트 기여도 정리

---

## 📌 **2. 기여한 부분**

---

## 1️⃣ 사용자 상태 관리 시스템

> 💡 **핵심 기술**: React Hooks, localStorage, 상태 관리

### 🎯 **도입 배경**

여러 사용자가 각자의 버킷리스트를 관리할 수 있어야 했습니다. 사용자를 선택하면 해당 사용자의 목록만 보이고, 새로고침해도 선택한 사용자가 유지되어야 했습니다.

### ✅ **구현 내용**

**👥 사용자 선택 UI (Header.jsx)**
- 최선호, 하다민, 홍유민 3명의 사용자 버튼
- 선택된 사용자 하이라이트 표시
- 직관적인 버튼 인터페이스

**💾 localStorage 기반 상태 영속성**
```javascript
localStorage.setItem("selectedUser", JSON.stringify(user))
```
- 새로고침해도 선택한 사용자 유지
- JSON 직렬화/역직렬화 활용

**🔄 중앙화된 상태 관리 (App.jsx)**
- `selectedUser` state로 현재 선택된 사용자 관리
- 사용자별 버킷리스트 필터링 (`filteredTodos`)
- 단방향 데이터 흐름 유지

---

## 2️⃣ RESTful API 백엔드 구현

> 💡 **핵심 기술**: Node.js, Express, MongoDB, Mongoose

### 🎯 **[도입 배경]**

버킷리스트 데이터를 서버에 저장하고 관리하기 위해 완전한 CRUD API가 필요했습니다.

### ✅ **[구현 내용]**

- 🗄️ **MongoDB 연결 및 Mongoose 스키마 설계**
  - `Bucket.js` 모델: name, goal, text, uid, isCompleted, date 필드
  - timestamps 자동 생성
- 🔧 **bucketRoutes.js - RESTful API 엔드포인트**
  - `POST /api/buckets` - 버킷리스트 생성
  - `GET /api/buckets` - 전체 목록 조회 (최신순 정렬)
  - `GET /api/buckets/:id` - 단일 항목 조회
  - `PUT /api/buckets/:id` - 전체 수정
  - `DELETE /api/buckets/:id` - 삭제
  - `PATCH /api/buckets/:id/check` - 완료 상태 토글
  - `PATCH /api/buckets/:id/text` - 텍스트만 수정
- ✅ **유효성 검사**
  - ObjectId 검증 함수 (`ensureObjectId`)
  - 필수 필드 검증
  - 에러 핸들링 및 적절한 HTTP 상태 코드 반환
- 🔐 **CORS 설정**
  - 환경변수 기반 origin 설정
  - credentials 지원

---

## 3️⃣ 게스트 인증 시스템

> 💡 **핵심 기술**: JWT 대체, 토큰 기반 인증

### 🎯 **도입 배경**

간단한 토큰 기반 게스트 인증 시스템이 필요했습니다.

### ✅ **구현 내용**

**🎫 게스트 토큰 생성 (authRoutes.js)**
```javascript
// POST /api/auth/guest
const guestToken = Math.random().toString(36).substring(2);
res.status(200).json({ token: guestToken });
```

**🔑 주요 기능**
- 랜덤 토큰 생성 알고리즘
- JSON 응답 형식
- 클라이언트 저장 준비 완료

---

## 4️⃣ 버킷리스트 CRUD UI 구현

> 💡 **핵심 기술**: React Components, Inline Editing, Form Handling

### 🎯 **도입 배경**

사용자가 직관적으로 버킷리스트를 추가, 조회, 수정, 삭제할 수 있는 UI가 필요했습니다.

### ✅ **구현 내용**

**✍️ BucketForm.jsx - 버킷리스트 추가 폼**
- ✨ 입력 필드 및 추가 버튼
- 🚨 사용자 선택 검증 (미선택 시 alert)
- 🛡️ 빈 값 입력 방지
- ⌨️ Enter 키 제출 지원

**📋 BucketList.jsx - 목록 컨테이너**
- 📭 목록이 없을 때 안내 메시지 표시
- 🔄 각 아이템을 BucketItem으로 렌더링
- 🎨 반응형 레이아웃

**📝 BucketItem.jsx - 개별 아이템**
- 👁️ 텍스트 표시 모드
- ✏️ 수정 모드 토글 (inline editing)
- 💾 수정/취소 버튼 (수정 모드)
- 🗑️ 삭제 버튼
- 🔧 상태 관리: `isEditing`, `editText`

```javascript
const [isEditing, setIsEditing] = useState(false);
const [editText, setEditText] = useState(todo.text);
```

---

## 5️⃣ 비동기 데이터 통신 (Fetch API)

> 💡 **핵심 기술**: Fetch API, Promise, 비동기 처리

### 🎯 **도입 배경**

프론트엔드와 백엔드 간 효율적인 데이터 통신이 필요했습니다.

### ✅ **구현 내용**

**🔄 App.jsx에서 Fetch API 활용**

| 메서드 | 기능 | 함수명 |
|--------|------|--------|
| `GET` | 초기 데이터 로딩 | `useEffect` |
| `POST` | 새 버킷리스트 생성 | `onCreate` |
| `DELETE` | 버킷리스트 삭제 | `onDelete` |
| `PATCH` | 텍스트 수정 | `onUpdate` |

**🐛 에러 핸들링**
```javascript
fetch(API_URL)
  .then(res => res.json())
  .then(data => setTodos(data))
  .catch(err => console.error('데이터 불러오기 실패:', err));
```
- `catch` 블록으로 에러 콘솔 출력
- 사용자 친화적 에러 메시지

**🔁 낙관적 UI 업데이트**
- ⚡ 서버 응답 후 state 즉시 갱신
- 🎯 삭제 시 필터링으로 UI 즉시 반영
- 🚀 빠른 사용자 경험 제공

---

## 6️⃣ 컴포넌트 기반 아키텍처

> 💡 **핵심 기술**: React Components, Props, 단방향 데이터 흐름

### 🎯 **도입 배경**

재사용 가능하고 유지보수가 쉬운 컴포넌트 구조가 필요했습니다.

### ✅ **구현 내용**

**🏗️ 컴포넌트 분리 전략**

```
📦 App.jsx (루트 컴포넌트)
├── 👤 Header.jsx          → 사용자 선택 UI
├── ✍️ BucketForm.jsx      → 입력 폼
└── 📋 BucketList.jsx      → 목록 컨테이너
    └── 📝 BucketItem.jsx  → 개별 아이템
```

**🔗 Props 전달 패턴**

**App → Header**
- `users` (사용자 목록)
- `selectedUser` (선택된 사용자)
- `onSelectUser` (선택 핸들러)

**App → BucketForm**
- `onCreate` (생성 함수)
- `selectedUser` (선택된 사용자)

**App → BucketList → BucketItem**
- `todos` (버킷리스트 배열)
- `onDelete` (삭제 함수)
- `onUpdate` (수정 함수)

**📁 CSS 모듈화**
- ✅ 각 컴포넌트별 독립적인 CSS 파일
- 📂 `App.css`, `Header.css`, `BucketForm.css`, `BucketItem.css`
- 🎨 스타일 충돌 방지 및 유지보수성 향상

---

## 7️⃣ 환경변수 및 배포 설정

> 💡 **핵심 기술**: dotenv, 환경 분리, npm scripts

### 🎯 **도입 배경**

개발/운영 환경을 분리하고 민감한 정보를 보호해야 했습니다.

### ✅ **구현 내용**

**🔐 .env 파일 관리**

| 환경변수 | 설명 | 예시 |
|----------|------|------|
| `MONGO_URI` | MongoDB 연결 문자열 | `mongodb://localhost:27017/bucket` |
| `PORT` | 서버 포트 | `3000` (기본값) |
| `FRONT_ORIGIN` | CORS 허용 도메인 | `http://localhost:5173` |

**🚀 package.json 스크립트**

**Backend**
```json
{
  "scripts": {
    "dev": "nodemon index.js"  // 자동 재시작
  }
}
```

**Frontend**
```json
{
  "scripts": {
    "dev": "vite",              // 개발 서버
    "build": "vite build"       // 프로덕션 빌드
  }
}
```

---

## 📊 **3. GitHub 기여도**

---

### 📝 커밋 내역 (주요 기여 분야)

**🔧 백엔드 개발**
- ⚙️ Express 초기 설정
- 🐚 MongoDB 연결 구성
- 🔗 CORS 설정
- 📚 Bucket 모델 스키마 설계
- 🚀 RESTful API 구현 (CRUD + 부분 수정)
- 🎫 게스트 인증 시스템 (authRoutes)

**🎨 프론트엔드 개발**
- 🏗️ 컴포넌트 구조 설계
- 👤 사용자 선택 기능
- 💾 localStorage 연동
- 🌐 Fetch API 통한 서버 통신
- 📑 UI 컴포넌트 구현 (Header, BucketForm, BucketList, BucketItem)
- 🎨 스타일링 및 UX 개선

### ✅ 주요 기능

⭐ **핵심 기능 구현 현황**

- ✅ 사용자별 버킷리스트 관리 시스템
- ✅ 완전한 CRUD 기능 (생성/조회/수정/삭제)
- ✅ Inline 편집 기능
- ✅ localStorage 기반 상태 유지
- ✅ MongoDB + Mongoose 데이터 영속성
- ✅ RESTful API 설계 및 구현
- ✅ CORS 및 환경변수 관리

**🔗 GitHub 링크**: https://github.com/hym-dot/mern-team-project

---

## 💡 **4. 기술적 이해도**

---

### 4.1 🎣 React Hooks (useState, useEffect)

**📍 구현 파일**: App.jsx, BucketItem.jsx

**📖 설명**

```javascript
// 상태 관리
const [todos, setTodos] = useState([]);
const [selectedUser, setSelectedUser] = useState(null);
const [isEditing, setIsEditing] = useState(false);

// 부수 효과
useEffect(() => {
  // 초기 데이터 로딩
  fetch(API_URL)
    .then(res => res.json())
    .then(data => setTodos(data));
}, []); // 빈 배열 = 컴포넌트 마운트 시 한 번만 실행
```

**📌 핵심 포인트**
- ✅ `useState`로 todos, selectedUser, isEditing 등의 로컬 상태 관리
- ✅ `useEffect`로 초기 데이터 로딩 및 localStorage에서 사용자 정보 복원
- ✅ 의존성 배열을 활용한 효율적인 side effect 관리

---

### 4.2 🔗 Props Drilling 패턴

**📍 구현 흐름**: App → Header/BucketForm/BucketList → BucketItem

**📖 설명**

```javascript
// App.jsx�에서 상태 관리
const [todos, setTodos] = useState([]);
const [selectedUser, setSelectedUser] = useState(null);

// 하위 컴포넌트로 Props 전달
<BucketList 
  todos={filteredTodos}
  onDelete={onDelete}
  onUpdate={onUpdate}
/>
```

**📌 핵심 포인트**
- ✅ 부모 컴포넌트(App)에서 상태 관리 및 핸들러 함수 정의
- ✅ Props를 통해 자식 컴포넌트로 데이터와 함수 전달
- ✅ 단방향 데이터 흐름 유지

---

### 4.3 🌐 RESTful API 설계

**📍 구현 파일**: bucketRoutes.js

**📖 설명** 
- HTTP 메서드 활용 (GET/POST/PUT/PATCH/DELETE)
- 적절한 상태 코드 반환 (200, 201, 400, 404)
- 리소스 중심 URL 설계 (`/api/buckets/:id`)
- PATCH를 활용한 부분 업데이트 (`/check`, `/text`)

### 4.4 Mongoose ODM

**구현**: Bucket.js

**설명**

```javascript
const bucketSchema = new mongoose.Schema({
  name: { type: String, required: true, trim: true },
  goal: { type: String, required: true, trim: true },
  uid: { type: String, required: true },
  isCompleted: { type: Boolean, default: false }
}, { timestamps: true });
```

**📌 핵심 포인트**
- ✅ 스키마 정의로 데이터 구조 명확화
- ✅ `required`, `default`, `trim` 등의 검증 및 변환 기능
- ✅ `timestamps` 옵션으로 createdAt/updatedAt 자동 관리
- ✅ `findByIdAndUpdate`, `findByIdAndDelete` 등 편리한 메서드 활용

---

### 4.5 💾 localStorage를 활용한 클라이언트 상태 영속성

**📍 구현 파일**: App.jsx의 handleUserSelect

**📖 설명** 
- `localStorage.setItem()`으로 사용자 선택 저장
- `localStorage.getItem()`으로 새로고침 후 복원
- JSON.stringify/JSON.parse로 객체 직렬화/역직렬화

### 4.6 Fetch API를 통한 비동기 통신

**구현**: App.jsx의 onCreate, onDelete, onUpdate

**📖 설명**

```javascript
fetch(API_URL, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(newBucket)
})
  .then(res => res.json())
  .then(data => setTodos([data, ...todos]))
  .catch(err => console.error('에러:', err));
```

**📌 핵심 포인트**
- ✅ Promise 기반 비동기 처리
- ✅ `then` 체이닝으로 응답 처리
- ✅ `catch`로 에러 핸들링
- ✅ 적절한 HTTP 헤더 설정 (`Content-Type: application/json`)

---

## 🤝 **5. 협업 및 문서화**

---

### 📁 프로젝트 구조

```
🗂️ mern-team-project/
├── 🔧 backend/
│   ├── index.js              # Express 서버 진입점
│   ├── .env                  # 환경변수
│   ├── models/
│   │   └── Bucket.js         # Mongoose 스키마
│   └── routes/
│       ├── authRoutes.js     # 게스트 인증 API
│       └── bucketRoutes.js   # 버킷리스트 CRUD API
└── 🎨 frontend/
    ├── package.json
    └── src/
        ├── App.jsx           # 루트 컴포넌트
        ├── main.jsx          # 진입점
        └── components/
            ├── Header.jsx
            ├── BucketForm.jsx
            ├── BucketList.jsx
            └── BucketItem.jsx
```

### 👥 팀원 구성

| 이름 | UID | 역할 |
|------|-----|------|
| 최선호 | user1 | 팀원 |
| 하다민 | user2 | 팀원 |
| 홍유민 | user3 | 팀원 |

### ⚙️ 개발 환경 설정

**Backend Tech Stack**
- 🟢 Node.js
- 🚂 Express
- 🍃 MongoDB
- 📦 Mongoose

**Frontend Tech Stack**
- ⚛️ React 19
- ⚡ Vite
- 📝 JavaScript (ES6+)

**개발 서버 실행**
```bash
# Backend
cd backend && npm run dev  # nodemon으로 자동 재시작

# Frontend
cd frontend && npm run dev  # Vite 개발 서버
```

---

## 🔧 **6. 문제 해결 사례**

---

### 6.1 🐛 ObjectId 유효성 검사 부재

**❌ 문제**: 잘못된 ID 형식으로 API 호출 시 서버 크래시

**🔍 원인**: MongoDB ObjectId 형식이 아닌 값이 들어올 경우 예외 발생

**✅ 해결**: 
- `ensureObjectId` 헬퍼 함수 구현
- `mongoose.isValidObjectId(id)` 검증 추가
- 400 Bad Request 반환으로 명확한 에러 응답

```javascript
const ensureObjectId = (id, res) => {
    if (!mongoose.isValidObjectId(id)) {
        res.status(400).json({ message: "유효하지 않은 ID형식입니다." });
        return false;
    }
    return true;
};
```

### 6.2 사용자 선택 없이 버킷리스트 추가 시도

**문제**: 사용자 선택 안 하고 추가하면 uid가 null로 저장됨

**원인**: selectedUser 상태 검증 부재

**해결**

```javascript
const handleSubmit = (e) => {
    e.preventDefault();
    if (!selectedUser) {
        alert("사용자를 먼저 선택해주세요.");
        return;  // early return
    }
    if (!input.trim()) return;
    onCreate(input);
};
```

**💡 배운 점**
- 폼 제출 시 선행 검증 필요
- 사용자에게 명확한 피드백 제공

---

### 6.3 ✏️ 수정 모드에서 빈 값 저장 가능

**❌ 문제**: 텍스트를 공백으로 수정하면 빈 버킷리스트가 저장됨

**🔍 원인**: trim() 검증 부재

**✅ 해결**:

```javascript
// Frontend - BucketItem.jsx
const handleSave = () => {
    if (editText.trim()) {  // trim() 검증
        onUpdate(todo._id, editText.trim());
        setIsEditing(false);
    }
};

// Backend - bucketRoutes.js
if (!text || !text.trim()) {
    return res.status(400).json({ message: "text는 필수입니다." });
}
```

**💡 배운 점**
- 프론트/백엔드 둘 다에서 검증 필요 (Double Validation)

---

### 6.4 🚫 CORS 에러

**❌ 문제**: 프론트엔드에서 백엔드 API 호출 시 CORS 에러

**🔍 원인**: Express에 CORS 설정 누락

**✅ 해결**:

```javascript
// backend/index.js
const cors = require('cors');

app.use(cors({
    origin: process.env.FRONT_ORIGIN,  // .env에서 관리
    credentials: true
}));
```

**💡 배운 점**
- CORS 정책 이해
- 환경변수로 보안 강화

---

### 6.5 🔄 새로고침 시 사용자 선택 초기화

**❌ 문제**: 페이지 새로고침하면 선택한 사용자가 사라짐

**🔍 원인**: 상태가 메모리에만 존재

**✅ 해결**:

```javascript
// 저장
const handleUserSelect = (user) => {
    setSelectedUser(user);
    localStorage.setItem("selectedUser", JSON.stringify(user));
};

// 복원
useEffect(() => {
    const savedUser = localStorage.getItem("selectedUser");
    if (savedUser) {
        setSelectedUser(JSON.parse(savedUser));
    }
}, []);
```

**💡 배운 점**
- localStorage 활용한 상태 영속성
- useEffect 의존성 배열 이해

---

## 📈 **7. 성과 및 개선사항**

---

### ✅ 달성한 목표

**🎯 프로젝트 완성도**

| 항목 | 상태 | 설명 |
|------|------|------|
| MERN 스택 구축 | ✅ | MongoDB + Express + React + Node.js |
| CRUD 기능 | ✅ | 생성/조회/수정/삭제 완벽 구현 |
| 사용자별 필터링 | ✅ | localStorage 기반 상태 관리 |
| RESTful API | ✅ | 7개 엔드포인트 구현 |
| React Hooks | ✅ | useState, useEffect 활용 |
| 컴포넌트 설계 | ✅ | 재사용 가능한 구조 |

### 🚀 개선 가능한 부분

**단기 개선 사항**
- 🔐 JWT 기반 실제 사용자 인증 시스템 구현
- 🎨 더 나은 UI/UX 디자인 (Material-UI, Tailwind CSS)
- ⚡ React Context API로 Props Drilling 개선
- 🧪 Jest/React Testing Library로 테스트 코드 작성

**중기 개선 사항**
- 🔄 SWR/React Query로 서버 상태 관리 개선
- 📱 반응형 디자인 최적화
- ♿ 접근성 개선 (ARIA 속성, 키보드 네비게이션)
- 🐛 에러 바운더리 추가

**장기 개선 사항**
- 📊 사용자별 통계 및 대시보드
- 🗂️ 카테고리/태그 기능 추가
- 🔔 알림 기능
- 🌙 다크 모드 지원

---

## 📚 **8. 학습 및 성장**

---

### 💻 기술 스택 습득

**Frontend**
- ⚛️ React 19
- 🎣 React Hooks (useState, useEffect)
- ⚡ Vite
- 📝 JavaScript (ES6+)

**Backend**
- 🟢 Node.js
- 🚂 Express
- 🍃 MongoDB
- 📦 Mongoose

**Tools**
- 📦 npm
- 🔄 nodemon
- 🔐 dotenv
- 🌿 Git

### 🎓 핵심 개념 이해

**Architecture & Patterns**

**Architecture & Patterns**
- ✅ 컴포넌트 기반 개발 (Component-Based Architecture)
- ✅ 단방향 데이터 흐름 (Unidirectional Data Flow)
- ✅ Props와 State 관리
- ✅ RESTful API 설계 원칙
- ✅ MVC 패턴의 이해

**Web Technologies**
- ✅ HTTP 메서드와 상태 코드
- ✅ MongoDB NoSQL 데이터베이스 개념
- ✅ Mongoose ODM (Object Data Modeling)
- ✅ 비동기 프로그래밍 (Promises, then/catch)
- ✅ localStorage를 활용한 브라우저 스토리지
- ✅ CORS와 보안 개념

### 🛠️ 문제 해결 능력 향상

**Debugging Skills**
- 🐛 콘솔 로그 활용한 디버깅
- 🔍 네트워크 탭으로 API 요청 분석
- 🚨 에러 메시지 해석 능력
- 📖 공식 문서 읽기 및 적용

**Problem Solving**
- 🧠 논리적 사고와 문제 분해 능력
- 🔄 이슈 추적 및 재현
- 💡 창의적 해결 방법 모색
- 📝 해결 과정 문서화

### 👥 협업 경험

**Team Collaboration**
- 팀 프로젝트 경험 (3인)
- Git을 활용한 버전 관리
- 코드 리뷰 및 피드백
- 프로젝트 구조 설계 및 역할 분담

---

## 🎯 **9. 향후 계획**

---

### 📅 단기 목표 (1-2개월)

1. **TypeScript 도입**
   - 타입 안정성 확보
   - 인터페이스 설계

2. **전역 상태 관리**
   - React Context API 구현
   - Props Drilling 해결

3. **기능 확장**
   - 완료/미완료 필터링
   - 우선순위 설정
   - 마감일 기능

### 📆 중기 목표 (3-6개월)

1. **인증 시스템 강화**
   - JWT 기반 인증
   - 이메일/소셜 로그인

2. **고급 기능**
   - 이미지 업로드 (S3, Cloudinary)
   - 소셜 공유 기능
   - 알림 시스템

3. **배포 및 최적화**
   - PWA(Progressive Web App) 변환
   - 서버리스 배포 (Vercel, Railway)
   - 성능 최적화

### 🚀 장기 목표 (6개월 이상)

1. **모바일 확장**
   - React Native 앱 개발
   - 크로스 플랫폼 지원

2. **아키텍처 진화**
   - GraphQL API 전환
   - 마이크로서비스 학습

3. **DevOps**
   - Docker/Kubernetes 기반 배포
   - CI/CD 파이프라인 구축
   - 모니터링 시스템 구축

---

## 📝 **10. 마무리**

---

### 🎓 프로젝트를 통해 얻은 것

이 프로젝트를 통해 **MERN 스택의 전체 흐름**을 이해하고, **풀스택 개발자로서의 기초 역량**을 갖추게 되었습니다.

**주요 성과**
- 🏗️ 프론트엔드와 백엔드를 모두 직접 구현
- 📊 데이터 흐름과 저장 방식 체득
- 🐛 실전 문제 해결 경험
- 👥 팀 협업 프로세스 이해

### 💪 앞으로의 다짐

> **"지속적인 학습과 개선을 통해 더 나은 개발자가 되겠습니다!"**

**Next Steps**
1. 코드 리팩토링 및 최적화
2. 테스트 코드 작성 습관화
3. 새로운 기술 스택 도전
4. 오픈소스 기여 시작

---

<div align="center">

**🌟 감사합니다! 🌟**

*작성일: 2025년 12월 29일*

[![GitHub](https://img.shields.io/badge/GitHub-mern--team--project-181717?style=for-the-badge&logo=github)](https://github.com/hym-dot/mern-team-project)

</div>
